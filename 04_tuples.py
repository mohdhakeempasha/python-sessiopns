# Creating a tuple using I()
t = (1, 2, 4, 5)

# Printing the element of a tuple 
#print(t[0])
# t1 = () # empty tuple 
# t1 = (1) # wrong way to declare a tuple with single element 
t1 = (1,) # Tuple with single element 
print(t1)
# Cannot update the value of a tuple 
#t[0] = 34